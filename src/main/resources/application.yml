spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION}  # in ms

server:
  port: ${SERVER_PORT}
